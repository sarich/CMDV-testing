pipeline {
    agent any
    stages {
        stage('Deploy') {
	    steps {
	        echo "Running 'Deploy' stage.."
            }
        }		
        stage('Test') {
            steps {
                echo "Running 'Test' stage..."
            }
        }
    }
}
